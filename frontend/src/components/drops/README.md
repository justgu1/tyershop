# Drop templates

Use estes caminhos para customizar a PDP por drop/produto:

- `components/drops/<drop>/<produto>.astro`
- fallback: `components/drops/<drop>.astro`

Props recebidas pelo componente custom:

- `product`
- `lang`
- `related`
- `newest`
